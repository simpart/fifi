#include <stdio.h>
#include "cap.h"
#include "fil.h"

int main (int argc, char *argv[]) {
    argc = argc;
    argv = argv;
    
    //printf("hello\n");
    /* start app */
    capture(filter);
    
    return 0;
}
